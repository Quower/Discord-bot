{
  "name": "guildoptions",
  "display": "bot options",
  "description": "some bot options",
  "settings": [
    {
      "name": "enableNewCommand",
      "display": "Enable new command by default",
      "description": "Should we enable new commands added by default(this feature is currently not implemented)",
      "type": "boolean",
      "defaultValue": true
    }
  ]
}
