import { Client, Message, SelectMenuInteraction } from "discord.js"
import { Model } from "mongoose"


export default async (options: {client: Client, message: Message, data: any}) => {
    //code
  } 