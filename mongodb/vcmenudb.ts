/*const mongoose = require('mongoose');

let VcMenu = false
let VcMenuwhitelist = false
let VcMenuMemberList = new Array()
let MenuVC: any
let addingMembers = false
let removingMembers = false
let menuMessage: any

const vcmenudata = new mongoose.Schema({
    VcMenu : {
        type : mongoose.SchemaTypes.Boolean,
        required : true,
        default : false
    },

    VcMenuwhitelist : {
        type : mongoose.SchemaTypes.Boolean,
        required : true,
        default : false
    },

    VcMenuMemberList : {
        type : mongoose.SchemaTypes.Array,
        required : true,
    },
    MenuVC : {
        type : mongoose.SchemaTypes.any,
        required : true
    },

    addingMembers : {
        type : mongoose.SchemaTypes.Boolean,
        required : true,
        default : false
    },
    removingMembers : {
        type : mongoose.SchemaTypes.Boolean,
        required : true,
        default : false
    },

    menuMessage : {
        type : mongoose.SchemaTypes.any,
        required : true
    }
})

module.exports = mongoose.model('VcMenuData', vcmenudata)*/